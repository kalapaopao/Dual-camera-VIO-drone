# 底盘串口通信协议 v1.0

## 1. 协议用途

本协议用于视觉计算平台 NX 向底盘电控发送线速度和角速度指令。

本协议仿照现有哨兵串口协议设计：固定帧头、两个 `int16_t` 小端数据、末尾 8 位累加和。

通信方向：

```text
NX → 底盘电控
```

第一版只定义单向底盘控制，不定义 ACK、状态回传、序号和复杂故障帧。

---

## 2. 串口参数

| 参数 | 设置 |
|---|---|
| 波特率 | 115200 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 |
| 流控 | 无 |
| 串口格式 | 115200 8N1 |
| 多字节字节序 | 小端序 Little Endian |

接线：

```text
NX/USB串口 TX → 电控 UART RX
NX/USB串口 RX → 电控 UART TX
NX/USB串口 GND → 电控 GND
```

必须确认 USB 转串口和电控 UART 的电平兼容。3.3V TTL、5V TTL 和 RS-232 不可以直接混接。

---

## 3. 数据帧格式

每一帧固定 6 字节：

```text
字节0       字节1       字节2       字节3       字节4       字节5
0x53        V_L         V_H         W_L         W_H         CHECKSUM
帧头        线速度低位  线速度高位  角速度低位  角速度高位  校验和
```

完整表示：

```text
53 V_L V_H W_L W_H CHECKSUM
```

| 字节 | 字段 | 类型 | 说明 |
|---:|---|---|---|
| 0 | Header | `uint8_t` | 固定为 `0x53` |
| 1-2 | V | `int16_t` | 线速度，单位 mm/s，小端序 |
| 3-4 | W | `int16_t` | 角速度，单位 mrad/s，小端序 |
| 5 | Checksum | `uint8_t` | 字节0到字节4累加后取低8位 |

---

## 4. 速度和方向定义

```text
V > 0：机器人前进
V < 0：机器人后退
V = 0：没有前后移动
```

```text
W > 0：机器人左转，逆时针
W < 0：机器人右转，顺时针
W = 0：直行或停止
```

速度范围：

```text
V：-500 ～ +500 mm/s
W：-1500 ～ +1500 mrad/s
```

电控必须进行范围检查，超范围数据不得直接驱动电机。

### 差速底盘换算

如果底盘是差速底盘，电控可使用：

```text
left  = v - w × wheel_distance / 2
right = v + w × wheel_distance / 2
```

计算前：

```text
v = V / 1000.0       单位：m/s
w = W / 1000.0       单位：rad/s
```

`wheel_distance` 是左右轮中心距离，单位为米，由电控根据实际底盘填写。

---

## 5. 校验和算法

校验和为前 5 个字节累加后的低 8 位：

```c
uint8_t checksum = 0;

for (int i = 0; i < 5; i++) {
    checksum = checksum + frame[i];
}

frame[5] = checksum;
```

等价表达：

```text
CHECKSUM = (byte0 + byte1 + byte2 + byte3 + byte4) % 256
```

接收端必须校验：

1. 字节0是否为 `0x53`；
2. 字节5是否等于前5字节累加和；
3. V和W是否在允许范围内。

任何一项不满足，都不得执行该帧运动指令。

---

## 6. 标准测试帧

### 6.1 前进 100 mm/s

```text
V = 100
W = 0

53 64 00 00 00 B7
```

计算：

```text
0x53 + 0x64 + 0x00 + 0x00 + 0x00 = 0xB7
```

### 6.2 原地左转

```text
V = 0
W = 100

53 00 00 64 00 B7
```

### 6.3 原地右转

```text
V = 0
W = -100

53 00 00 9C FF EE
```

### 6.4 停止

```text
V = 0
W = 0

53 00 00 00 00 53
```

停止帧没有单独的模式字段，V和W都为0即代表停止。

---

## 7. 电控安全要求

电控必须在本地实现通信看门狗：

```text
连续 300 ms 没有收到有效数据帧 → 自动停止底盘
```

以下情况必须停止或保持停止：

- 串口通信中断；
- 300 ms 未收到有效帧；
- 帧头错误；
- 校验和错误；
- V超出范围；
- W超出范围；
- 电控自身检测到故障；
- 收到停止帧。

300 ms 看门狗必须由电控独立实现，不能依赖 NX 程序正常退出。

---

## 8. 接收程序参考流程

```text
等待接收 0x53
    ↓
继续接收4个数据字节和1个校验字节
    ↓
计算前5字节累加和
    ↓
校验通过？
    ├── 否：丢弃该帧，保持停车或保持上一安全状态
    └── 是：检查V/W范围并执行指令
```

接收错误数据后，程序必须重新寻找下一个 `0x53`，不能永久卡在错误状态。

---

## 9. 第一版联调顺序

1. 电控先用 PC 或单元测试程序解析标准测试帧；
2. 不连接电机，只打印解析出的 V、W 和校验结果；
3. 架空底盘，测试停止、前进、左转和右转；
4. 拔掉串口，验证 300 ms 内自动停车；
5. 地面低速测试，建议 V 不超过 100 mm/s、W 不超过 200 mrad/s；
6. 最后再接入视觉导航。

首次测试必须准备物理急停和电机断电开关。

---

## 10. NX 当前配置

```text
NX端口：/dev/ttyACM0
波特率：115200
协议：6字节 0x53 底盘速度帧
```

NX 程序默认处于安全模式，不发送真实串口数据：

```yaml
dry_run: true
enable_motion: false
```

电控完成协议实现并通过架空测试后，NX 端才允许切换到真实发送模式。
